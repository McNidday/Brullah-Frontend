import classNames from "classnames";
import styles from "./styles.module.scss";

const Footer = () => {
  return (
    <footer className={classNames(styles.footer)}>Nidday is awesome</footer>
  );
};

export default Footer;
