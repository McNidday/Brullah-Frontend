import cn from "classnames";
import styles from "./styles.module.scss";

const DashboardTransactions = () => {
  return (
    <div className={cn(styles.container)}>
      <div className={cn(styles.miniContainer)}></div>
    </div>
  );
};

export default DashboardTransactions;
